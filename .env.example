# Neon Postgres connection string (pooled)
DATABASE_URL=

# Auth.js session secret — generate with: npx auth secret
AUTH_SECRET=

# Allow new account registration. Set to "false" after you create your account.
ALLOW_SIGNUPS=true
